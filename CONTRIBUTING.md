# Contributing

Open an issue before a substantial change. Pull requests should identify the cron semantics affected, add deterministic tests, preserve offline operation and accessibility, and pass the test, syntax, and build commands. New dialect syntax must be explicit rather than silently changing five-field behavior.
