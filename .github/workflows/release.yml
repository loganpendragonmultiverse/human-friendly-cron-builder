name: Release build
on:
  push: { tags: ["v*"] }
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version: 24 }
      - run: npm test
      - run: npm run build
      - name: Package
        run: cd dist/site && zip -r ../human-friendly-cron-builder-1.0.0.zip .
      - uses: actions/upload-artifact@v7
        with:
          name: human-friendly-cron-builder-1.0.0
          path: dist/human-friendly-cron-builder-1.0.0.zip
